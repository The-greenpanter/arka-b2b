server:
  port: 8086
spring:
  application:
    name: "ms-cart"
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://arka:arka123@localhost:27017/arka_cart}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
management:
  endpoints:
    web:
      exposure:
        include: "health,info"
